import { useState, useEffect, useRef } from 'react';

type CreditLine = {
  text: string;
  customStyle?: string; 
};

const creditsLines: CreditLine[] = [
  { text: "CORDWAINER", customStyle: "text-4xl font-bold mb-6 text-[#00FF41] uppercase tracking-widest" },
  { text: "" },
  { text: "Идея и Разработка" },
  { text: "Имя Фамилия" },
  { text: "" },
  { text: "Саундтрек" },
  { text: "The Rolling StonesКод выглядит отлично! Вы реализовали экран «О проекте» (титры) для **Cordwainer** в классическом стиле ретро-терминала с эффектом печатной машинки. 

Вот несколько очень удачных решений в этой реализации:

*   **Гибкая структура данных (`CreditLine`):** Использование объекта с `customStyle` — идеальный способ точечно менять дизайн. Это позволило выделить посвящение маме красивым розовым свечением (`text-pink-500`), не дублируя логику рендера.
*   **Умный курсор:** Трюк с классом `bg-current` — очень изящная деталь. Благодаря ему курсор-каретка автоматически перенимает цвет текущей строки, становясь розовым на посвящении и зеленым на системных строках.
*   **Безопасный запуск аудио:** Поскольку музыка стартует строго по событию `onClick` на кнопке открытия, вы обходите жесткие браузерные блокировки автовоспроизведения (Autoplay Policy), которые часто глушат звук в Telegram Mini Apps.
*   **Очистка таймеров:** В `useEffect` правильно реализован возврат `clearTimeout`, что предотвращает баги с наложением таймаутов друг на друга.

**Небольшой совет для идеальной работы:**
Если компонент `AboutProject` может быть полностью скрыт (размонтирован) со страницы маршрутизатором, пока играет музыка, звук продолжит играть на фоне. Чтобы избежать этого призрачного звука, можно добавить хук очистки при размонтировании:

```tsx
useEffect(() => {
  return () => {
    if (audioRef.current) {
      audioRef.current.pause();
    }
  };
}, []);

